// wipe database


// takes recipes object complete or not, add it tlo the database

    // if complete (ingredients_need_per_serving = 0): add to database
    // if complete (ingredients_need_per_serving !=): scan db to ensure v similar item isn't there if not add to database